---
description: A Toast component.
---

import { ToastMessage } from './toast';

A component that does something special and renders text in a div.

### Component usage

```js
<ToastMessage type="error" message="faile, retry" />
```

### Render hello world!

```js live
<ToastMessage type="error" message="faile, retry" />
```
